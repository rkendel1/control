# Daily-driver completion audit

This is a requirements ledger, not a completion claim. A row is complete only when
the current implementation and the cited verification both cover the user-visible
workflow.

| Requirement | Current evidence | Status |
|---|---|---|
| One durable source of truth | `control-db.ts` constructs exactly one active FeltDB runtime from explicit topology bootstrap: durable IndexedDB locally or authenticated FeltDB HTTP remotely. The competing web/JSON daemon, UI graph, compatibility feature, and native JSON state have been removed; topology-neutral snapshots preserve identities. Packaged IndexedDB checks and an authenticated real-adapter HTTP round trip both pass | Verified local and HTTP adapter contracts |
| Canonical Work graph | Every task owns stable Work identity; Runs, Run events, conversations, decisions, activity, evidence, and outcomes link to that Work. Schema v13 adds an idempotent reconciliation on every startup: it repairs missing Task/conversation/investigation Work and mismatched task-linked artifact identity, records repair/issue counts in FeltDB metadata, and exposes graph health in Activity. The packaged self-test deliberately breaks and repairs ownership, then proves the graph and zero-residue cleanup | Verified |
| Investigations and evidence inspection | Schema-v3 durable investigations capture hypotheses, sources, contradictions, gaps, and conclusions; investigation conversations receive the full Work context; Evidence view exposes provenance and outcomes; packaged self-test proved persistence and cleanup | Verified |
| Inbox and handoff | Schema-v4 FeltDB inbox items durably represent assignments, agent handoffs, questions, reviews, escalations, and feedback with sender, recipient, Work, task, lifecycle, and provenance. Agents can emit a structured handoff marker; Control validates the target, limits a Work to six automatic hops, reassigns it, supplies the shared inbox context, and dispatches the next agent. Invalid, cyclic, exhausted, or failed dispatches escalate to the human inbox. The packaged self-test proves the protocol, handoff identity, action lifecycle, graph linkage, and zero-residue cleanup | Verified |
| Human and agent identity | Schema-v5 FeltDB participants provide a stable local human principal plus synchronized persistent agent principals. Messages, Inbox actions, feedback, and activity reference participant identity; legacy `me` relationships migrate to the durable local principal. The packaged self-test proved human and agent identity persistence | Verified local topology |
| Intelligence execution policy | Every Intelligence profile owns durable filesystem/shell/network and 1–240 minute timeout policy. The native boundary fail-closes runtime selection, applies runtime sandboxes/tool constraints, and hard-kills timed-out children before emitting failure. Production discovery and supervised execution support Claude Code, Codex, OpenCode, and Ollama (`ollama:model`, `CONTROL_OLLAMA_MODEL`, or first installed model). Coding-capable Ollama models use Ollama's official headless Claude Code integration for workspace-scoped Read/Edit/Write/Bash tools; observe-only use remains available. Automatic routing is local-first when this integration is ready. Native policy/model parsing tests pass; a live coding run still requires a running Ollama service and suitable model. | Implemented; live environment verification required |
| Data topology and migration | Local and authenticated server modes use the same Control collections and types. Schema v6 migrates legacy names safely. Switching copies the complete graph before saving configuration; Activity exposes topology and recovery. A localhost authenticated FeltDB protocol harness proved remote schema initialization, graph export/delete/restore, and stable identities through the installed HTTP adapter | Verified contract; deployed service operations pending |
| Secure credentials | FeltDB server tokens are excluded from FeltDB, backups, UI state, and configuration files and stored through the native OS credential service. Packaged macOS Keychain write/read/delete passed; HTTPS is required except localhost | Verified on macOS; Windows/Linux release gates pending |
| Projects | Native folder validation; CLI/folder opening and `--forget-project`; FeltDB registry; switch/remove. Explicit launch paths are resolved inside the same workspace initialization transaction and take precedence over persisted selection. Canonical graph removal stops project PTYs, cascades messages plus every project-scoped collection, preserves shared agents/settings, and verifies zero residue without touching source. The rebuilt `.app` was launched with a different fixture while another project was persisted, and the requested fixture rendered active with its complete tree | Verified on macOS |
| Files and editor | Deep tree, dotfiles, optional vendor tree, create/rename/delete, Monaco read/save, external refresh, and recovery drafts. Quick Open (Cmd/Ctrl+P) indexes the live nested tree with keyboard/path-term filtering instead of requiring an exact typed path. Search results carry line/column navigation into Monaco, including repeated navigation in an already-open file. Explorer and workspace Search share the same generated/vendor “All” visibility setting; hidden files remain searchable while `.git` internals stay excluded. A lifecycle-managed recursive native watcher emits workspace-relative create/modify/remove/rename events; the UI debounces them into explorer/Git refreshes and immediately refreshes clean editors while preserving dirty buffers with a visible conflict warning. Pending drafts flush during project changes; dirty close/switch/remove actions require confirmation; Save detects disk changes since load before overwriting. Quick-open/navigation unit coverage, native watcher integration, packaged rendered Quick Open, and packaged hidden/generated search plus live-event checks pass | Verified on macOS |
| Terminal | Real xterm-compatible emulator over a persistent PTY per tab with raw keyboard input, ANSI/full-screen rendering, shell-native completion/history, live resize, durable active-tab/cwd/redacted history state, per-session serialized writes, explicit tab close, Ctrl-C, and Unix/Windows process-tree cleanup. Run Tests detects npm/pnpm/yarn/bun, Cargo, Python, Go, or Make, creates a test terminal when none exists, waits for shell readiness, and surfaces errors instead of silently dropping the command; native and packaged integration prove `xterm-256color`, resize, cwd continuity, interrupt survival, and toolchain detection on macOS | Verified on macOS; Windows/Linux release gates pending |
| Git | Status, mixed staged/unstaged state, tracked/staged/untracked diffs, workspace-boundary validation, deletion staging, stage/unstage, initial/subsequent commit, history, branches, async fetch/pull/push, first-push upstream setup | Verified by local remote workflow tests |
| Tasks and triage | Capture creates Work; assignment, priority, filtering/sorting, dependencies, acceptance criteria, tags, archive, dispatch; shared dispatch prevents concurrent duplicate runs. Completion, approval, reopen, archive, and restore are canonical FeltDB operations: they synchronize Task and Work, preserve archive provenance, and append durable Evidence, Outcome, and Activity records. One runtime transition matrix now owns dispatch start/failure, review, decision pause/resume, handoff, stop, and crash-recovery Task/Work state. Executable lifecycle tests cover that matrix plus success→review, failure, decision pause, and orphan recovery; isolated packaged self-test proves review→approval→reopen→approval→archive→restore using real FeltDB | Verified |
| Real agents | Claude/Codex/OpenCode discovery includes authentication health; auto routing skips expired Claude auth; exact Codex execution contract returned `CONTROL_RUNTIME_OK`; streamed runs, stop/recovery, configurable agents | Codex live path verified; UI lifecycle implemented |
| Conversations | Project/task conversations retain message delivery state and errors and always own canonical Work context. Schema v12 moves each response onto a supervised Run with runtime, PID/start-time identity, configurable timeout, streamed events, stop/recovery handling, evidence, Activity, and actionable failure escalation. Failed user messages remain visibly retryable without duplication; fast completion cannot be overwritten by launch state. Exact read-only Codex chat contract returned `CONTROL_CHAT_OK`, and packaged graph checks prove message↔Run↔Conversation↔Work identity | Verified locally |
| Decisions, evidence, outcomes | Agent decision marker, pending inbox, answers durably resume paused tasks. Schema-v7 run artifacts capture completion-time Git changes relative to the dirty baseline plus structured agent-reported tests. Approval creates passed human-review evidence and an outcome; feedback closes the prior review action, creates a reopened outcome, notifies the assigned agent, and automatically dispatches the revision. Dispatch failures create a durable human escalation | Verified locally |
| Persistent run history and crash recovery | Per-Run serialization preserves event order; FeltDB stores redacted output content in append-only chunks, flushes partial final lines on completion/stop, and retains selectable timelines plus output/error/exit/runtime/task/agent context. Runs durably retain PID plus OS process start time; restart reconciliation verifies both before terminating an orphaned process tree, rejects PID reuse, reopens Work, and creates an actionable Inbox escalation. Controlled `.app` replacement retained records; native identity tests and the isolated packaged self-test verify the durable identity schema and task-linked Run events/activity | Verified on macOS |
| Packaging | Debug and optimized release macOS `.app` bundles build without a development server. The exact release bundle passes all 39 isolated packaged checks, including filesystem/Git, persistent PTY, rendered Quick Open/xterm, Keychain, runtime discovery and live Ollama, FeltDB lifecycle, conversations, decisions, evidence, export/restore, and cleanup. A real release window was captured successfully | Verified unsigned release on macOS; signing/notarization pending |
| Shell reachability | Command palette and keyboard shortcuts reach project, file, search, terminal, test, task/agent, and Git surfaces; recoverable operation errors are dismissible and do not replace the Workbench | Implemented; macOS assistive-access click automation unavailable |
| Production state hygiene | Shipping surfaces use FeltDB/native state; retired panel components no longer fabricate agent runs or compiler problems; production entrypoints contain no seeded demo records | Verified by source audit and production build |
| Repository integrity | `control-repository-audit.json` inventories every tracked and non-ignored file with purpose, classification, reachability, authority, and disposition. The approved cleanup reduced the present repository from 550 to 150 files, removed the complete legacy web/JSON architecture plus unreachable UI/native stacks, reconciled docs/tooling/dependencies/IPC, and leaves 150/150 audited with zero unknown, legacy, dead, or delete dispositions. Post-cleanup frontend 18/18, native 14/14, production build, bundle build, and packaged 39/39 all pass | Verified locally |

The current debug package passes the implementation, native integration, live runtime,
restart, upgrade, rendered-window, and isolated packaged workflow checks in this ledger.
The packaged self-test is invoked with `--self-test-project=<path>` and
`--self-test-report=<path>`, currently passes 39 checks, and removes all temporary
FeltDB records (including Inbox actions) in a `finally` block.

## Remaining blocking GA gates

- Prove the packaged daily-driver workflows on Windows and Linux, including PTY/process-tree behavior, recursive watching, secure credential storage, runtime discovery, restart recovery, and installation/upgrade.
- Exercise an actual deployed authenticated FeltDB service through migration, outage/recovery, export/restore, and multi-device operation; the adapter contract is verified, but deployed operations are not.
- Run clean-machine installation and upgrade acceptance for release-signed artifacts. The optimized macOS `.app` is verified locally, but this machine has no valid Apple code-signing identity.
